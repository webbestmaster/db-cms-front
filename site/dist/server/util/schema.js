"use strict";
/*
import {Schema, Validator} from 'jsonschema';

import {DocumentType} from '../data-base-cms-type';

const validator = new Validator();

export function getIsValid(data: DocumentType, schema: Schema): boolean {
    return validator.validate(data, {...schema, additionalProperties: false}, {allowUnknownAttributes: false}).valid;
}
*/
